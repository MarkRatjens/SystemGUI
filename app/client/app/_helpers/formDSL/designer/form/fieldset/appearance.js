app.formDSL.designer.form.fieldset.appearance = (f) => [

  f.field( {
    key: 'label',
    horizontal: true,
  } ),

  f.field( {
    key: 'legend',
    horizontal: true,
  } ),

  f.field( {
    key: 'horizontal',
    horizontal: true,
    as: 'checkbox',
  } ),

]
