// app.dashboard.blueprints.arenas = (router) => (a, x) => [
//
//   app.http({
//     url: '/api/arenas',
//     success: (arenas, el) => {
//       el.$nodes = arenas.map(
//         (arena) => app.dashboard.blueprints.provisioning(router, arena)
//       )
//     }
//   }),
//
// ]
