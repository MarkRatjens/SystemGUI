// app.dashboard.blueprints.provisioning.delete = (router, arena) => (a, x) => [
//
//   app.http({
//     url: `/api/provisioning/${arena}/${router.params.blueprint_id}`,
//     method: 'Delete',
//     placeholder: app.spinner('Removing provisions'),
//     success: () => router.open('..'),
//   }),
//
// ]
