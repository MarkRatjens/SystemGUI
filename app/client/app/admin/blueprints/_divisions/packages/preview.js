// app.admin.blueprints.packages.preview = (router, blueprint) => (a,x) =>
// blueprint.packages ?
// [
//   'Packages',
//   a.ul(
//     blueprint.packages.map(pckage => a.li(pckage.descriptor.repository))
//   ),
// ] : a._
